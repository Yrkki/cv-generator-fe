import { GeneralTimelineEntry } from './general-timeline-entry';

describe('GeneralTimelineEntry', () => {
  it('should create an instance', () => {
    expect(new GeneralTimelineEntry()).toBeTruthy();
  });
});
