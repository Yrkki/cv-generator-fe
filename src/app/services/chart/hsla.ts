/** Hue, saturation, lightness and alpha based color structure. */
export class HSLA {
    /** Hue. */
    h: any;
    /** Saturation. */
    s: any;
    /** Lightness. */
    l: any;
    /** Alpha. */
    a: any;
}
